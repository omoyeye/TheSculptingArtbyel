import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { useCallback } from "react";

export default function BrandStory() {
  // Return null to remove the section from the Home page
  return null;
}
